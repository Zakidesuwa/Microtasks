specVersion: "v1"
serviceId: "microtasks"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "microtasks-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
